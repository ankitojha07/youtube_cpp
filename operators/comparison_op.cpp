#include <iostream>
using namespace std;
int main()
{
    int a, b, c;
    a = 9;
    b = 8;
    // comparison operator
    // cout << (a < b)
    // equality operator (==)
    // cout << (a == b);

    // not equal
    // cout << (a != b);

    // greater or equal
    cout << (a >= b) << endl;

    // less or equal
    cout << (a <= b);
    return 0;
}